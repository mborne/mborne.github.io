fn main() {
    let mut d = 12.0;
    println!("d={}",d);
    /*
     * Note that :
     * - mut is required to allow assignation
     * - the same type must be assigned
     */
    d = 15.0;
    println!("d={}",d);
}
